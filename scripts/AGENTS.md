# Purpose

`scripts/` contains small support scripts for `pxp` development and evaluation.

Scripts are not runtime CLI capabilities. Keep them offline, deterministic, and aligned with the command contracts.
